package initialize

/*import (
	"gochat/global"
	"strconv"
)

type server interface {
	ListenAndServe() error
}

func RunCode() {
	//systemconfig := global.Config.System
	//addr := systemconfig.Host + ":" + strconv.Itoa(systemconfig.Port)

}*/
